# Kronos AI Web
Premium, lightweight market-analysis interface. Runs entirely in the browser and supports OHLCV CSV input, charting, forecast horizon selection, screenshot attachment and baseline backtesting UI.

The numerical demo is intentionally transparent: true original Kronos inference requires a Python/PyTorch runtime and is not silently substituted by an API.